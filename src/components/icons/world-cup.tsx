import { IoTrophyOutline } from 'react-icons/io5'

export function WorldCupIcon({ className, size = 24 }: { className?: string; size?: number }) {
  return <IoTrophyOutline className={className} size={size} />
}